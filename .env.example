CURSOR_API_KEY=cursor_...

# Optional — if set, insight visuals use OpenAI Images (DALL·E).
# Without it, the app falls back to Pollinations (no key required).
# OPENAI_API_KEY=sk-...
# OPENAI_IMAGE_MODEL=dall-e-3
